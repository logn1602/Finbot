services:
  finbot:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Persist ChromaDB across container restarts
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
